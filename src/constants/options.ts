import { Options } from "../types.d";

// =============================================================================

const defaultOptions: Options = {
	regexp: false,
	global: true,
	multiline: true,
	caseSensitive: false,
};

export default defaultOptions;
